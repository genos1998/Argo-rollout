apiVersion: v1
kind: ConfigMap
metadata:
  name: elastic-verifier
data:
  elastic-verifier: |
    monitoringProvider: ELASTICSEARCH
    accountName: logEalastic1
    index: kubernetes*
    responseKeywords: log
